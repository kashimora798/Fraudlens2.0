# PostgreSQL connection string used by drizzle-orm (src/db/index.ts)
# Required at runtime -- the app throws "DATABASE_URL is required" without it.
# In Vercel: Project Settings -> Environment Variables -> add DATABASE_URL there.
# Do NOT put your real credentials in this file; this is just a template.
DATABASE_URL=postgresql://user:password@host:5432/database
